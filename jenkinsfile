pipeline {
    agent any
    tools {
        maven 'Tu Maven'
    }
    stages{
        stage('checking') {
            steps {
                git branch: 'w6-inclass', url: 'https://github.com/tuMetropolia/Calculator.git'
            }
        }

        stage('build'){
            steps{
                sh 'mvn clean install'
            }
        }

        stage('Test & Coverage') {
                    steps {
                        bat 'mvn test jacoco:report' // Runs tests & generates JaCoCo coverage report
                    }
                    post {
                        always {
                            junit 'target/surefire-reports/*.xml' // Publish JUnit test results
                            jacoco execPattern: '**/target/jacoco.exec', // Reads JaCoCo execution file
                                   classPattern: '**/target/classes',
                                   sourcePattern: '**/src/main/java',
                                   exclusionPattern: '**/test/**'
                        }
                    }

     }

}
}